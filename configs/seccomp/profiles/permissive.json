{
  "defaultAction": "SCMP_ACT_ALLOW",
  "comment": "WARNING: This profile effectively disables seccomp sandboxing. The agent container can invoke arbitrary syscalls including ptrace, mount, unshare, and setns — all of which are container-escape primitives. Use ONLY for temporary debugging of syscall-related failures. Switch back to 'standard' or 'hardened' immediately after. NEVER use in production.",
  "syscalls": [
    {
      "names": ["socket"],
      "action": "SCMP_ACT_ERRNO",
      "errnoRet": 1,
      "args": [
        { "index": 0, "value": 3, "op": "SCMP_CMP_EQ" }
      ],
      "comment": "Block AF_RAW sockets to prevent proxy bypass"
    }
  ]
}
